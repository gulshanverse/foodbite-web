export type PaymentInitialization = { provider: string; providerOrderId: string | null; amount: number; currency: string; configured: boolean };
export interface PaymentProvider { readonly name: string; createPaymentOrder(input: { orderId: string; amount: number; currency: string }): Promise<PaymentInitialization>; verifyPayment(input: { orderId: string; providerPaymentId: string; signature: string }): Promise<boolean>; refundPayment(input: { providerPaymentId: string; amount: number }): Promise<{ accepted: boolean }>; }

class UnconfiguredPaymentProvider implements PaymentProvider { readonly name = "UNCONFIGURED"; async createPaymentOrder(input: { orderId: string; amount: number; currency: string }) { return { provider: this.name, providerOrderId: null, amount: input.amount, currency: input.currency, configured: false }; } async verifyPayment() { return false; } async refundPayment() { return { accepted: false }; } }
class DeterministicTestPaymentProvider implements PaymentProvider { readonly name = "TEST"; async createPaymentOrder(input: { orderId: string; amount: number; currency: string }) { return { provider: this.name, providerOrderId: `test_order_${input.orderId}`, amount: input.amount, currency: input.currency, configured: true }; } async verifyPayment() { return true; } async refundPayment() { return { accepted: true }; } }
export function getPaymentProvider(): PaymentProvider { return process.env.NODE_ENV === "test" ? new DeterministicTestPaymentProvider() : new UnconfiguredPaymentProvider(); }
