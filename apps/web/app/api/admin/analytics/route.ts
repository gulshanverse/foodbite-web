import { NextResponse } from "next/server";
import { requirePermission } from "@/lib/authorization";
import { getAdminAnalytics, analyticsCsv } from "@/lib/analytics-domain";
export async function GET(request: Request) { const gate = await requirePermission("ANALYTICS_READ"); if (gate.response === "unauthenticated") return NextResponse.json({ error: "Authentication required." }, { status: 401 }); if (gate.response) return NextResponse.json({ error: "Forbidden." }, { status: 403 }); try { const params = Object.fromEntries(new URL(request.url).searchParams.entries()); const data = await getAdminAnalytics(params); if (new URL(request.url).searchParams.get("format") === "csv") return new NextResponse(analyticsCsv(data), { headers: { "content-type": "text/csv; charset=utf-8", "content-disposition": "attachment; filename=foodbite-platform-analytics.csv", "cache-control": "no-store" } }); return NextResponse.json(data); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Analytics unavailable." }, { status: 400 }); } }
