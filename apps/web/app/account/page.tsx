import Link from "next/link";
import { ArrowRight, LogOut, ShieldCheck, UserRound } from "lucide-react";
import { requireAuth } from "@/lib/authorization";

export default async function AccountPage() { const user = await requireAuth(); const profileHref = user.role === "SELLER" ? "/seller/profile" : user.role === "NGO" ? "/ngo/profile" : "/buyer/profile"; const workspaceHref = user.role === "SELLER" ? "/seller" : user.role === "NGO" ? "/ngo" : "/explore"; return <main className="product-page"><section className="hero-panel px-6 py-8 sm:px-9 sm:py-10"><div className="relative z-10 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="page-kicker">Your account</p><h1 className="mt-3 text-4xl font-black sm:text-5xl">Welcome to FoodBite</h1><p className="mt-3">Your account is ready. Choose a workspace below to continue.</p></div><span className="status-chip" data-tone="accent"><ShieldCheck className="size-3.5" />{user.role}</span></div></section><div className="mt-6 grid gap-5 md:grid-cols-2"><section className="surface-card p-6 sm:p-7"><span className="grid size-11 place-items-center rounded-2xl bg-[var(--accent-soft)] text-[var(--accent)]"><UserRound className="size-5" /></span><p className="page-kicker mt-5">Account details</p><h2 className="mt-2 text-xl font-black">{user.email}</h2><p className="mt-2 text-sm">Role: <strong>{user.role}</strong></p><div className="mt-6 flex flex-wrap gap-2"><Link href={profileHref} className="inline-flex items-center gap-2 rounded-xl bg-[var(--foreground)] px-4 py-3 text-sm font-black text-[var(--background)]">Open profile <ArrowRight className="size-4" /></Link><Link href="/api/auth/signout" className="inline-flex items-center gap-2 rounded-xl border border-[var(--border)] px-4 py-3 text-sm font-bold"><LogOut className="size-4" />Log out</Link></div></section><section className="surface-card p-6 sm:p-7"><p className="page-kicker">Continue</p><h2 className="mt-2 text-xl font-black">Open your {user.role.toLowerCase()} workspace</h2><p className="mt-3 text-sm leading-6">Jump back into the part of FoodBite built around your role.</p><Link href={workspaceHref} className="mt-6 inline-flex items-center gap-2 font-black">Continue <ArrowRight className="size-4 text-[var(--accent)]" /></Link></section></div></main>; }
