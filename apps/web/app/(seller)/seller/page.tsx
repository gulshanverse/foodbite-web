import Link from "next/link";
import { requireRole } from "@/lib/authorization";
import { getSellerOperationalSummary } from "@/lib/seller-operations-domain";
export default async function SellerDashboardPage() { const user = await requireRole("SELLER"); const summary = await getSellerOperationalSummary(user.id); const cards = [["Active listings", summary.listings.active], ["Paused listings", summary.listings.paused], ["Expiring in 24h", summary.listings.expiringSoon], ["Available surplus", summary.inventory.availableQuantity ?? 0], ["Low-stock listings", summary.listings.lowStock]]; return <main className="mx-auto max-w-6xl px-6 py-12"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-[#e85d3f]">Seller operations</p><h1 className="mt-3 text-4xl font-bold text-[#173f3b]">Surplus workspace</h1><p className="mt-3 text-[#55706c]">Real-time operational information from your listings, inventory, orders, and donations.</p></div><div className="flex gap-3"><Link href="/seller/business" className="rounded-lg border border-[#173f3b] px-5 py-3 text-center font-semibold text-[#173f3b]">Business settings</Link><Link href="/seller/listings/new" className="rounded-lg bg-[#e85d3f] px-5 py-3 text-center font-semibold text-white">Add surplus food</Link></div></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{cards.map(([label, value]) => <div key={String(label)} className="rounded-xl border border-[#dbe4df] bg-white p-5"><p className="text-sm text-[#55706c]">{label}</p><p className="mt-3 text-3xl font-bold text-[#173f3b]">{value}</p></div>)}</div><div className="mt-10 grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-[#dbe4df] bg-white p-6"><h2 className="text-xl font-bold text-[#173f3b]">Order operations</h2><div className="mt-5 grid grid-cols-2 gap-3 text-sm">{Object.entries(summary.orders).map(([status, count]) => <div key={status} className="rounded-lg bg-[#eef2ef] p-3"><p className="text-[#55706c]">{status.replaceAll("_", " ")}</p><p className="mt-1 text-xl font-bold text-[#173f3b]">{count}</p></div>)}</div><Link href="/seller/orders" className="mt-5 inline-block font-semibold underline">Manage orders</Link></section><section className="rounded-2xl border border-[#dbe4df] bg-white p-6"><h2 className="text-xl font-bold text-[#173f3b]">Donation recovery</h2><div className="mt-5 grid grid-cols-2 gap-3 text-sm">{Object.entries(summary.donations).map(([status, count]) => <div key={status} className="rounded-lg bg-[#fff1e9] p-3"><p className="text-[#7b3b2e]">{status.replaceAll("_", " ")}</p><p className="mt-1 text-xl font-bold text-[#173f3b]">{count}</p></div>)}</div><Link href="/seller/donations" className="mt-5 inline-block font-semibold underline">Manage donations</Link></section></div><section className="mt-6 rounded-2xl border border-[#dbe4df] bg-white p-6"><h2 className="text-xl font-bold text-[#173f3b]">Inventory accounting</h2><p className="mt-3 text-sm text-[#55706c]">Available {summary.inventory.availableQuantity ?? 0} · Reserved {summary.inventory.reservedQuantity ?? 0} · Sold {summary.inventory.soldQuantity ?? 0} · Donated {summary.inventory.donatedQuantity ?? 0}</p><p className="mt-3 text-xs text-[#8a9b96]">Food information is provided by sellers. FoodBite does not certify food safety.</p></section></main>; }
