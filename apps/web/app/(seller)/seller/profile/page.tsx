import { requireRole } from "@/lib/authorization";
export default async function SellerProfilePage() { const user = await requireRole("SELLER"); return <main className="mx-auto max-w-3xl px-6 py-16"><p className="text-sm font-semibold uppercase tracking-[.18em] text-[#e85d3f]">Seller profile</p><h1 className="mt-3 text-4xl font-bold text-[#173f3b]">Your seller profile</h1><p className="mt-4 text-[#55706c]">{user.email}</p><p className="mt-8 rounded-xl border border-[#dbe4df] bg-white p-5 text-sm text-[#55706c]">Seller verification is pending future onboarding workflows. No seller is represented as verified.</p></main>; }
