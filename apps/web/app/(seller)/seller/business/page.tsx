import { requireRole } from "@/lib/authorization";
import { prisma } from "@/lib/prisma";
import { BusinessForm } from "@/components/seller/seller-forms";

export default async function SellerBusinessPage() { const user = await requireRole("SELLER"); const seller = await prisma.sellerProfile.findUnique({ where: { userId: user.id }, include: { business: true } }); const business = seller?.business; return <main className="mx-auto max-w-4xl px-6 py-12"><p className="text-sm font-semibold uppercase tracking-[.18em] text-[#e85d3f]">Seller business</p><h1 className="mt-3 text-4xl font-bold text-[#173f3b]">Business information</h1><p className="mt-3 max-w-2xl text-[#55706c]">Keep your business details current. Seller-provided FSSAI information is collected for future verification workflows and does not mean your business is verified.</p><div className="mt-10 rounded-2xl border border-[#dbe4df] bg-white p-6"><BusinessForm initial={business ? { name: business.name, type: business.type ?? "RESTAURANT", description: business.description, phone: business.phone, email: business.email, address: business.address, city: business.city, state: business.state, pincode: business.pincode, fssaiNumber: business.fssaiNumber } : undefined} /></div></main>; }
