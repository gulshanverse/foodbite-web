import { requireRole } from "@/lib/authorization";
import { prisma } from "@/lib/prisma";

export default async function SellerInventoryPage() { const user = await requireRole("SELLER"); const seller = await prisma.sellerProfile.findUnique({ where: { userId: user.id }, select: { id: true } }); const inventory = seller ? await prisma.inventory.findMany({ where: { listing: { sellerId: seller.id } }, include: { listing: { select: { name: true, status: true, unit: true } } }, orderBy: { updatedAt: "desc" }, take: 50 }) : []; return <main className="mx-auto max-w-6xl px-6 py-12"><p className="text-sm font-semibold uppercase tracking-[.18em] text-[#e85d3f]">Seller inventory</p><h1 className="mt-3 text-4xl font-bold text-[#173f3b]">Inventory</h1><p className="mt-3 text-[#55706c]">Quantities are maintained through transactional domain operations.</p>{inventory.length === 0 ? <div className="mt-10 rounded-2xl border border-dashed border-[#b8cbc4] bg-white p-12 text-center"><h2 className="text-xl font-bold text-[#173f3b]">No inventory yet</h2><p className="mt-2 text-[#55706c]">Active listings will appear here with available, reserved, and sold quantities.</p></div> : <div className="mt-10 overflow-x-auto rounded-2xl border border-[#dbe4df] bg-white"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-[#dbe4df] text-[#55706c]"><tr><th className="p-4 font-medium">Listing</th><th className="p-4 font-medium">Total</th><th className="p-4 font-medium">Available</th><th className="p-4 font-medium">Reserved</th><th className="p-4 font-medium">Sold</th><th className="p-4 font-medium">Status</th></tr></thead><tbody>{inventory.map((item) => <tr key={item.id} className="border-b border-[#eef2ef] last:border-0"><td className="p-4 font-semibold text-[#173f3b]">{item.listing.name}<span className="block text-xs font-normal text-[#55706c]">{item.listing.unit.toLowerCase()}</span></td><td className="p-4">{item.totalQuantity}</td><td className="p-4">{item.availableQuantity}</td><td className="p-4">{item.reservedQuantity}</td><td className="p-4">{item.soldQuantity}</td><td className="p-4"><span className="rounded-full bg-[#eef2ef] px-3 py-1 text-xs font-semibold">{item.listing.status}</span></td></tr>)}</tbody></table></div>}</main>; }
