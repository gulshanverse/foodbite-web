"use client";
import { useState } from "react";
export function AddToCartButton({ listingId, available }: { listingId: string; available: number }) { const [quantity, setQuantity] = useState(1); const [message, setMessage] = useState(""); async function add() { const response = await fetch("/api/cart", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ listingId, quantity }) }); const data = await response.json() as { error?: string }; setMessage(response.ok ? "Added to cart." : data.error ?? "Could not add to cart."); } return <div className="mt-7 flex flex-wrap items-center gap-3"><select value={quantity} onChange={(event) => setQuantity(Number(event.target.value))} aria-label="Quantity" className="min-h-12 rounded-lg border border-[#dbe4df] px-3">{Array.from({ length: Math.min(available, 10) }, (_, index) => <option key={index + 1} value={index + 1}>{index + 1}</option>)}</select><button type="button" onClick={add} className="min-h-12 rounded-lg bg-[#173f3b] px-6 font-semibold text-white transition hover:bg-[#285c56] focus:outline-none focus:ring-2 focus:ring-[#e85d3f]">Add to cart</button>{message && <span role="status" className="text-sm font-semibold text-[#55706c]">{message}</span>}</div>; }
