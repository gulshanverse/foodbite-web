import Link from "next/link";
import { FavoriteButton } from "./favorite-button";

type CardListing = { id: string; slug: string; name: string; listingType: string; foodType: string; originalPrice: number; sellingPrice: number; pickupEnd: Date; quantity: number; unit: string; inventory: { availableQuantity: number } | null; category: { name: string; slug: string }; images: { url: string; altText: string }[]; seller: { business: { name: string; city: string | null; verificationStatus: string } | null }; discountPercent: number; distanceKm: number | null };
export function FoodCard({ listing }: { listing: CardListing }) { const image = listing.images[0]; return <article className="group overflow-hidden rounded-2xl border border-[#dbe4df] bg-white shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="relative aspect-[4/3] bg-[#f1dfc7]">{image ? <img src={image.url} alt={image.altText} className="h-full w-full object-cover" /> : <div className="grid h-full place-items-center text-sm font-semibold text-[#55706c]">Surplus food</div>}<div className="absolute left-3 top-3 rounded-full bg-white/90 px-3 py-1 text-xs font-bold text-[#173f3b]">{listing.discountPercent}% off</div><div className="absolute right-3 top-3"><FavoriteButton listingId={listing.id} /></div></div><div className="p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wide text-[#e85d3f]">{listing.category.name}</p><h2 className="mt-2 text-lg font-bold text-[#173f3b]">{listing.name}</h2><p className="mt-1 text-sm text-[#55706c]">{listing.seller.business?.name ?? "Local food business"}</p></div><span className="rounded-full bg-[#eef2ef] px-2 py-1 text-xs font-semibold">{listing.foodType.replaceAll("_", " ")}</span></div><div className="mt-5 flex items-end justify-between"><div><span className="text-xl font-bold text-[#173f3b]">₹{Math.round(listing.sellingPrice / 100)}</span><span className="ml-2 text-sm text-[#8a9b96] line-through">₹{Math.round(listing.originalPrice / 100)}</span></div><span className="text-sm font-semibold text-[#55706c]">{listing.inventory?.availableQuantity ?? 0} {listing.unit.toLowerCase()} left</span></div><div className="mt-4 flex items-center justify-between text-xs text-[#55706c]"><span>Pickup until {listing.pickupEnd.toLocaleTimeString("en-IN", { hour: "numeric", minute: "2-digit" })}</span><span>{listing.distanceKm === null ? listing.seller.business?.city ?? "Pickup" : `${listing.distanceKm.toFixed(1)} km away`}</span></div><Link href={`/food/${listing.slug}`} className="mt-5 block rounded-lg border border-[#173f3b] px-4 py-3 text-center text-sm font-semibold text-[#173f3b] transition hover:bg-[#eef2ef]">View details</Link></div></article>; }
