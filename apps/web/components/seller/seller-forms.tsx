"use client";
import React, { useEffect, useState } from "react";

export function BusinessForm({ initial }: { initial?: Record<string, string | null> }) { const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setPending(true); const data = Object.fromEntries([...new FormData(e.currentTarget)].filter(([, value]) => value !== "")); const response = await fetch("/api/seller/business", { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify(data) }); setMessage(response.ok ? "Business information saved." : "Please check the business details."); setPending(false); } return <form onSubmit={submit} className="grid gap-5 sm:grid-cols-2">{field("name", "Business name", initial?.name ?? "", true)}<label className="block text-sm font-medium">Business type<select name="type" defaultValue={initial?.type ?? "RESTAURANT"} className="mt-2 min-h-11 w-full rounded-lg border border-[#dbe4df] bg-white px-3"><option value="RESTAURANT">Restaurant</option><option value="HOTEL">Hotel</option><option value="BAKERY">Bakery</option><option value="CAFE">Café</option><option value="MESS">Mess</option><option value="CATERER">Caterer</option><option value="SUPERMARKET">Supermarket</option><option value="CLOUD_KITCHEN">Cloud kitchen</option><option value="OTHER">Other</option></select></label>{field("description", "Description", initial?.description ?? "", false, true)}{field("phone", "Phone", initial?.phone ?? "")}{field("email", "Business email", initial?.email ?? "", false, false, "email")}{field("address", "Address", initial?.address ?? "", false, true)}{field("city", "City", initial?.city ?? "")}{field("state", "State", initial?.state ?? "")}{field("pincode", "Pincode", initial?.pincode ?? "")}{field("fssaiNumber", "FSSAI number (seller-provided)", initial?.fssaiNumber ?? "")}<div className="sm:col-span-2">{message && <p role="status" className="mb-3 text-sm text-[#55706c]">{message}</p>}<button disabled={pending} className="min-h-11 rounded-lg bg-[#e85d3f] px-5 py-3 font-semibold text-white disabled:opacity-60">{pending ? "Saving…" : "Save business information"}</button></div></form>; }

export function ListingForm() { const [categories, setCategories] = useState<Array<{ id: string; name: string }>>([]); const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); useEffect(() => { void fetch("/api/seller/categories").then((r) => r.json()).then((d: { categories?: Array<{ id: string; name: string }> }) => setCategories(d.categories ?? [])); }, []); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setPending(true); const raw = Object.fromEntries(new FormData(e.currentTarget)); const data = { ...raw, originalPrice: Number(raw.originalPrice) * 100, sellingPrice: Number(raw.sellingPrice) * 100, quantity: Number(raw.quantity), pickupStart: new Date(String(raw.pickupStart)).toISOString(), pickupEnd: new Date(String(raw.pickupEnd)).toISOString() }; const response = await fetch("/api/seller/listings", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(data) }); setMessage(response.ok ? "Draft saved. Review and publish it from Listings." : "Please check the listing details."); setPending(false); } return <form onSubmit={submit} className="grid gap-5 sm:grid-cols-2">{field("name", "Food name", "", true)}<label className="block text-sm font-medium">Category<select name="categoryId" required className="mt-2 min-h-11 w-full rounded-lg border border-[#dbe4df] bg-white px-3">{categories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></label>{field("description", "Description", "", false, true)}<label className="block text-sm font-medium">Listing type<select name="listingType" className="mt-2 min-h-11 w-full rounded-lg border border-[#dbe4df] bg-white px-3"><option value="EXACT">Exact</option><option value="SURPRISE_BOX">Surprise box</option></select></label><label className="block text-sm font-medium">Food type<select name="foodType" className="mt-2 min-h-11 w-full rounded-lg border border-[#dbe4df] bg-white px-3"><option value="VEGETARIAN">Vegetarian</option><option value="NON_VEGETARIAN">Non-vegetarian</option><option value="VEGAN">Vegan</option><option value="JAIN">Jain</option><option value="OTHER">Other</option></select></label>{field("quantity", "Quantity", "", true, false, "number")}{field("unit", "Unit", "PORTION")}{field("originalPrice", "Original price (₹)", "", true, false, "number", "0.01")}{field("sellingPrice", "Surplus price (₹)", "", true, false, "number", "0.01")}{field("pickupStart", "Pickup starts", "", true, false, "datetime-local")}{field("pickupEnd", "Pickup ends", "", true, false, "datetime-local")}{field("packagingInfo", "Packaging information", "", false, true)}{field("allergenInfo", "Allergen information", "", false, true)}<div className="sm:col-span-2"><p className="mb-4 text-sm text-[#55706c]">Food information is provided by the seller. FoodBite does not guarantee food safety.</p>{message && <p role="status" className="mb-3 text-sm text-[#55706c]">{message}</p>}<button disabled={pending || categories.length === 0} className="min-h-11 rounded-lg bg-[#e85d3f] px-5 py-3 font-semibold text-white disabled:opacity-60">{pending ? "Saving…" : "Save draft"}</button></div></form>; }
function field(name: string, label: string, value = "", required = false, wide = false, type = "text", step?: string) { return <label className={`${wide ? "sm:col-span-2 " : ""}block text-sm font-medium`}>{label}<input name={name} defaultValue={value} required={required} type={type} step={step} className="mt-2 min-h-11 w-full rounded-lg border border-[#dbe4df] bg-white px-3" /></label>; }
