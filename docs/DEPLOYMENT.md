# Deployment

No deployment is configured in Phase 0. A future deployment must provide Node.js, the environment variables in `.env.example`, a PostgreSQL database, Prisma migration execution, and observability appropriate to the selected platform.
